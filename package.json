{
  "name": "interview-bot",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"node scripts/dev-api.js\" \"npm run dev --prefix frontend\"",
    "build": "npm run build --prefix frontend",
    "install-all": "npm install && npm install --prefix frontend"
  },
  "dependencies": {
    "@ai-sdk/openai": "^1.0.0",
    "ai": "^4.0.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.0",
    "mammoth": "^1.8.0",
    "pdf-parse": "^1.1.1",
    "word-extractor": "^1.0.4"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
